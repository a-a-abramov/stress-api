apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stress-api.fullname" . }}-test-connection"
  labels:
    {{- include "stress-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stress-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
